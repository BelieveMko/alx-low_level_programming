#include "main.h"

/**
 * rev_string - Reverses a string
 * @s: Input string
 * Return: String reversed
 */

void rev_string(char *s)
{
	int i = 0;
	int n = 0;


	while (s[i] != '\0')
	{
		i++;
	}

	while (i > 0)
	{
		s[c] = s[i];
		c++;

	}
