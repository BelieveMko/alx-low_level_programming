#include "main.h"
/**
 * swap_int - swaps the values of two integer variables a and b
 * @a: first integer
 * @b: second integer
 */
void swap_int(int *a, int *b)
{
	m = *a;
	n = *b;

	*a = n;
	*b = m;
}
