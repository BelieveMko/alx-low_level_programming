#include "main.h"
/**
 * _abs - this function computes the absolute value of an integer
 * @n: the integer to determine the absolute value
 * Return: - absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	else
	{
		return (-n);
	}
}
