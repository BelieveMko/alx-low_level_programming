#include "main.h"
/**
 * _islower - check if char is lowercase
 * @c: is a variable
 * Return: 1 if char is lowercase, otherwise 0.
 */
int _islower(int c)
{
	if (c <= 90)
	{
		return (49);
	}
	else if (c >= 97)
	{
		return (48);
	}
	else
	{
        }
	return (0);	
}

